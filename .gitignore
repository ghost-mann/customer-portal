.DS_Store
*.pyc
*.egg-info
*.swp
__pycache__
node_modules
dev-dist
build

# NOTE: Built frontend (customer_portal/public/frontend and customer_portal/www/*.html)
# is committed so Frappe Cloud serves the pages without a custom build step.
# Regenerate with `yarn build` in the Frontend/ dir, then commit the result.

.vscode
